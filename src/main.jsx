import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import "./index.css";

// Correct the typo and ensure you are accessing the DOM element correctly
ReactDOM.createRoot(document.getElementById('root')).render(<App />);
